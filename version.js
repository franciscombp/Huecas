/* ============================================================
   Versión de Huecas — ÚNICA fuente de verdad.
   La leen app.js (para el aviso de novedades) y sw.js (para
   nombrar la caché: cambiar la versión invalida la anterior).

   Al publicar una versión nueva: sube el número y escribe la
   nota en una línea, como para leerla en un toast.
   ============================================================ */

const APP_VERSION = 'v24';
const APP_NOTA = 'La cocina coral de las referencias, y la app ya se puede instalar y jugar sin internet.';
